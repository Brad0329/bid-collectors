# bid-collectors — 공공 입찰공고·지원사업 수집 파이썬 패키지

Python 3.11 · httpx(async) · Pydantic v2 · lxml · BeautifulSoup4로 만든 수집 라이브러리(v1.1.0). 소비자는 BidWatch.
(2026-09-23 greenfield 템플릿 체계를 기존 저장소에 이식 — Phase 001~003은 이식 전에 완료됐다.)

## 현재 단계
- 전체 Phase 체크리스트/아키텍처의 **단일 원본 = `work_log/plan.md`** (여기에 중복 기재 금지)
- 현재: **Phase 006 진행(2026-09-25 발급, v1.2.5) — 항목 잔여 결함 A·B + 원칙 ① `extra` 원문 전부** (Phase 완료 시 이 줄과 plan.md 체크박스를 함께 갱신)

## 새 세션 시작 시
- 훅(`session_context.py`)이 plan.md의 현재 Phase·읽을 Phase 로그·실테스트 대기를 주입한다 — 지시대로 읽는다.
  **세션 첫머리에 `[session_context]` 줄이 안 보이면 훅이 죽은 것이다** — `work_log/plan.md`부터 직접 읽고 훅을 고친다.

## 불변 규칙
- **Silent failure 절대 금지**: 모든 예외 상황을 명시적으로 로깅한다. 빈 except/catch 금지.
  넘어가야 할 정당한 이유가 있으면 그 이유를 로그+주석으로 남긴다.
  **이 패키지에서 "로그만 남기고 break"는 조용한 실패다** — 소비자는 로그를 못 본다. 실패는 `CollectResult.errors`/`is_partial`로 돌려준다.
- **조용한 절단 금지**: 결과를 상한으로 자르면 **잘랐다는 사실과 전체 건수를 함께** 돌려준다.
  페이지네이션 첫 페이지만 받기·라이브러리 기본 limit·로그 꼬리처럼 **암묵적으로 잘리는 것도 절단이다**(`max_pages`·content 500자).
- **손으로 만든 데이터에는 자동 검증을 붙인다**: 사람이 쓴 매핑·목록은 반드시 틀린다.
- **허용의 기준은 "위험"이다 — 파괴적이지 않으면 연다** (2026-08-22 템플릿 결정. 1인·서버 없음 전제 — 협업·운영 서버가 생기면 다시 묻는다).
  **루틴 작업(커밋·테스트·빌드·설치·조사)에 승인 창이 뜨면 그것이 결함이다.** 파괴적 명령은 deny,
  받은 실행물 실행·패키지 **추가**(pyproject에 없는 새 의존성)는 건별 확인 — pyproject대로 설치하는
  `pip install -e .[dev]`는 추가가 아니라 설치이므로 연다. 반복되면 ① 허용된 형태로 부른다(습관 — 반복되면 훅)
  ② 규칙 추가. 점검은 Phase 종료 시 `/approval-audit`. 대가·목록 실물: `노하우_승인_대기_최소화.md`
- **비밀정보(API 키)는 이 파일·커밋·채팅에 절대 쓰지 않는다.** `.env`(`DATA_GO_KR_KEY`·`BIZINFO_API_KEY`)에 두고 gitignore한다.
- **역할 경계**: 외부 사이트에서 공고를 가져오는 것은 전부 이 패키지 몫 / DB 저장·키워드 매칭·스케줄링·AI 설정 생성·캐싱은
  소비 서비스(BidWatch) 몫. 소비자 쪽 일을 여기서 받지 않는다. **bidwatch 폴더의 파일은 이 저장소 세션에서 수정하지 않는다**
  (읽기·grep·테스트 실행은 된다). 버전 작업을 끝내면 `docs/handover/v<버전>.md`(템플릿 `_TEMPLATE.md`)에 변경 상황과 bidwatch가
  할 일을 적어 넘기고, 반영은 bidwatch 세션이 한다(2026-09-25 사용자 결정).

## 소비자 계약 변경 게이트 ★계약은 되돌리기 비싸다 (템플릿의 '스키마 변경 게이트'를 이 패키지에 맞게)
- 이 패키지에는 DB가 없다 — 되돌리기 비싼 것은 **BidWatch가 기대하는 모양**이다. 계약 = `Notice`·`CollectResult`·
  `BaseCollector`·`ScraperConfig`·공개 export·수집기별 `bid_no` 형식(바뀌면 BidWatch upsert가 같은 공고를 중복 저장한다).
- 계약 **결정의 기록 = `docs/CONTRACT.md`**, 계약 명세 = `docs/interface.md`.
- **AI는 임의로 계약을 바꾸지 않는다.** ① 변경안(무엇을·왜·영향·버전)을 CONTRACT.md에 먼저 기록 → ② 사용자 확인 → ③ 반영.
- **필드·선택 인자 추가 = minor, 제거·이름 변경·타입 변경·bid_no 형식 변경 = major.** 버전은 `pyproject.toml`과 `__init__.py` 둘 다.
- **`docs/interface.md`는 bidwatch 저장소에 같은 문서가 있다 — 여기서 고치고 handover 문서로 넘긴다.** bidwatch 쪽 파일은 그쪽 세션이
  반영한다(2026-09-25까지는 양쪽을 여기서 맞췄다).
- 코드가 "현재 상태"의 정답이다. 문서와 어긋나면 문서를 고친다. `extra` 안의 키는 계약 밖이다.

## 작업 트랙 ★저위험이 기본값이다 — 기준은 "몇 줄이냐"가 아니라 "되돌리기가 비싼가"

전제: 1인 프로젝트(협업 시 재정의). 병목은 왕복 횟수다. 역사: `docs/playbooks/노하우_작업_트랙.md`

- **일반 트랙은 ① 데이터 ② 기본 구조에 닿을 때만** — 그 외 전부(새 수집기·파싱 수정·문구·설정, 새 기능 포함)는
  저위험. 애매하면 저위험으로 시작하고, 닿으면 멈추고 **사용자에게 묻는다** —
  혼자 일반 트랙으로 올리지 않는다. 검증은 사다리의 **일반 트랙 갈래**(소비자 영향 확인·되돌리기 경로).
  bid-collectors의 ②의 실물: **`docs/interface.md` 변경**(위 게이트 — handover로 전달) · `BaseCollector._fetch()` 템플릿 메서드 구조 ·
  기존 수집기의 `bid_no` 형식. ①(데이터)은 해당 없음 — 저장소가 없다.
- **저위험 실행**: 한 턴에 구현→검증(사다리대로)→**커밋**. 보고 10줄 이내, 커밋은
  묻지 않는다. 작업 중 발견한 무관 결함이 5분짜리면 그 자리에서 고친다(커밋만 나눈다).

## 작업 속도 규칙 ★실제로 샌 시간을 재고 정한다
- **느리다고 느껴지면 테스트가 아니라 승인 대기부터 잰다**(실측: 테스트 30초 vs 대기 6분).
  도구: `scripts/measure_wait.py`(벽시계 실측)·`measure_approvals.py`(원인 분류, 예측).
  처방: `docs/playbooks/노하우_승인_대기_최소화.md`
- **★ 검증 강도는 트랙이 아니라 "무엇을 바꿨나"로 정한다** (2026-09-05 템플릿 결정):
  문서·주석·문구(동작 불변) = diff 읽기, 코드 파일이면 정적 분석 1회, 테스트 0 /
  설정(동작 변경, 로직 아님) = 정적 분석 1 + 관련 테스트 1(관련 = 바뀐 파일을 직접 쓰는 테스트) /
  로직 변경 = 정적 분석 1 + 관련 테스트 1 / **변이 확인은 테스트를 새로 넣거나 고쳤을 때만**('검증 원칙') —
  변이는 코드가 아니라 테스트를 검증하는 것이라 테스트를 안 건드린 한 줄 수정에는 붙지 않는다. **기존 단언의
  기대값만 바꾼 것(20→10)도 붙지 않는다**(잡는지는 만들 때 봤다). **변이를 되돌린 확인은 재실행이 아니라 diff로** /
  커밋 직전 전체 실행 = 로직이 바뀐 커밋만. 생략에는 근거(의존 그래프). 외부 API 실측·문서 갱신은 줄이지 않는다.
  **일반 트랙(계약·기본 구조)은 종류가 다른 검증을 탄다**: 전체 테스트 + **BidWatch 쪽 테스트를 이 코드로 실제로 돌려 확인**
  (editable 설치라 즉시 반영된다) + 되돌리기 경로(이전 버전 태그·커밋) 확인.
- **★ 긴 명령이 곧 긴 승인 대기다**(38초 vs 83초 실측). 웹 페이지는 curl이 아니라 `WebFetch`로 /
  `python -c` 인라인·세션 UUID가 든 스크래치패드 절대경로 금지(훅) / 한글 인자를 셸 명령에 쓰지 않는다 /
  일회성 스크립트는 gitignore된 `scripts/_tmp/`에 덮어써서 쓰고 **지우지 않는다**(삭제는 매번 승인) /
  **일회성 명령은 "항상 허용"으로 열지 않는다** — 다시 안 걸리는 죽은 규칙만 남는다.
- **커밋은 작업 단위마다.** 여러 줄 메시지는 `.commit_msg.txt`에 쓰고 `git commit -F .commit_msg.txt`.
  검증 결과(테스트 범위·수용 기준 대조·**대체 수단을 썼으면 그 사실과 이유**)는
  Phase 로그가 아니라 **커밋 메시지에** 적는다 — 조용히 건너뛰지 않는다.

## 요구사항 정책 ★"정답 기준"이 없으면 완료를 판단할 수 없다
- 기능 요구사항 + 수용 기준의 **단일 원본 = `docs/REQUIREMENTS.md`**
- **구현 전**: 수용 기준이 먼저 있어야 한다 — 없으면 스펙화하고 모호점은 사용자에게 질문.
- **요구사항이 바뀌면 즉시 갱신** — 대화 속 합의로만 남기면 다음 세션이 구버전으로 구현한다.
- **수용 기준은 검증 가능한 문장으로**("X를 넣으면 Y가 N건") — 그대로 테스트 케이스가 된다.
- **YAGNI**: 문서에 없는 요구사항을 위한 추상화·설정화·일반화 금지.
- 한 번에 훑기 어려워지면(대략 기능 15~20개) 도메인별로 분할하고 REQUIREMENTS.md는 목차+링크 인덱스로.
- **상태 어휘를 구분한다: 미착수/진행/완료/동결/중단/폐기/안 함.** 보류와 "안 할 것"을 섞지
  않고, 닫은 결정에는 날짜·근거를 남긴다(조사 기록은 지우지 않는다). 상세: `노하우_프로세스.md`
- **요구사항 ID 대장 — 새 번호는 여기서 받아간다.** 아이디어 단계여도 번호를 잡았으면 적는다.
  **번호를 재사용하지 말 것.** 발급 시 plan.md 보류 항목의 후보 번호까지 훑는다(`노하우_프로세스.md`).
  새 번호는 **새 기능에만** — 앞 기능의 마무리·문구는 앞 절 한 줄 수정, 작은 기능은 한 줄 형태.

  | ID | 기능 | 상태 |
  |---|---|---|
  | F-001 | 나라장터 입찰공고 수집 | 완료 (부분 결과 보존 Phase 004) |
  | F-002 | 나라장터 확장 — 낙찰·계약·사전규격 | 완료 |
  | F-003 | K-Startup 사업공고 수집 | 완료 |
  | F-004 | 기업마당 지원사업 수집 | 완료 |
  | F-005 | 보조금24 공공서비스 수집 | 완료 (서버 필터 형식 수정 Phase 004) |
  | F-006 | 중소벤처기업부 사업공고 수집 | 완료 |
  | F-007 | GenericScraper — config 기반 HTML 게시판 수집 | 완료 (실패·절단 보고·요청 훅 Phase 004) |
  | F-008 | 상세 조회 fetch_detail | 완료 |
  | F-009 | 공통 유틸(날짜·HTML·상태·HTTP) | 완료 |
  | F-010 | 알리오 공공기관 입찰공고 수집 | 완료 (2026-09-24, v1.2.0) |
  | **다음 번호** | **F-011** | |

## 검증 원칙
- **"됐다"는 실제 결과로 판단한다.** 테스트 통과·import 성공은 수집이 된다는 근거가 아니다.
  외부 API 응답을 실제로 받아 건수·필드를 확인한다(표본 수와 파라미터를 함께 적는다).
- **기능 완료 시**: REQUIREMENTS.md의 수용 기준을 하나씩 짚어 대조하고 → **그 자리에서 REQUIREMENTS의
  상태와 plan.md를 둘 다 갱신한다.** 코드가 맞아도 정답지가 틀리면 다음 세션이 오독한다.
- **Phase 완료 시**: 전체 테스트 통과가 다음 Phase의 선행 조건. plan.md 체크·커밋·push까지 끝나면
  **"새 Phase는 새 세션을 열어 진행하기를 권고합니다"라고 말하고 멈춘다** — 다음 Phase를 이 세션에서
  시작하지 않는다. 이어받기는 plan.md와 Phase 로그가 한다.
- **새로 넣거나 고친 테스트는 실제로 무언가를 잡는지 확인한다** — 대상 코드를 일부러 틀리게 만들어
  실패를 한 번 본다(변이가 실제로 적용됐는지까지 확인, 한 번에 하나씩). "잘리지 않는다·빠지지 않는다"류는
  실제 값(건수)을 재야 잡는다 — 존재 확인은 잘려도 통과한다.
- **자동 테스트/코드 리뷰/실제 API 호출은 잡는 결함의 층이 다르다 — 서로 대체 불가.**
  고정 응답 테스트는 파싱만 검증한다. 실측에는 "고장 상태를 만들어야 드러나는" 시나리오(키 없음·쿼터 초과·빈 응답)를 넣는다.
- **표본 2건은 실측이 아니다** — 기록에 표본 수·확인 파라미터를 함께 적는다.
- BidWatch 연동 확인처럼 이 저장소에서 끝나지 않는 항목은 plan.md "사용자 실테스트 대기"에 — **확인 전 완료 선언 금지.**
- 상세(변이 확인법, 검증의 층, 디버깅 원칙): `docs/playbooks/노하우_검증.md`

## 검증 게이트 ★도구가 아니라 목적을 지킨다
- **게이트 4종은 도구가 없어도 건너뛰지 않는다** — 조사→`researcher`, 스펙화→`requirements-analyst`,
  수용 기준 대조→`spec-checker`, Phase 통합 테스트→`qa-tester`(foreground). 에이전트를 못 쓰면
  메인 agent가 **같은 원칙·같은 보고 형식으로 직접** 하고 그 사실을 커밋 메시지에 적는다
  (원칙은 각 `.claude/agents/*.md`에 있다). 수집기 단위 테스트는 별도 agent 없이 메인이 사다리대로 돌린다.
- **사용자 호출 스킬**: `/code-review`(못 하면 커밋 메시지에 남기고 넘어간다) · `/security-review`
  (시점은 '보안 3층' ②·③, `origin` 원격 필요, 없으면 직접 점검) · `/debt-audit`(**Phase 3~5개마다 사용자에게 시점을 알린다** —
  목록만 만들고 정리 여부는 사용자 결정).
- **넓은 조사는 서브에이전트 병렬로**(코드 탐색 `Explore`·`general-purpose`, 데이터·API 조사
  `researcher`) — 메인은 **결론만** 보관한다. PLAN MODE는 plan.md 최초 작성·스코프 변경 시.

## 보안 3층 ★매 커밋이 아니라 "무엇을 바꿨나"로 걸린다 (2026-09-09 템플릿 결정)
- **① 코딩 규칙 — 항상, 별도 단계 없음**: URL·셸·파일 경로에 입력을 문자열로 조합하지 않는다(쿼리는 `params=`) /
  API 키를 로그·예외 메시지·`CollectResult.errors`에 싣지 않는다(httpx 예외 문자열에 쿼리스트링이 들어간다) /
  비밀정보는 '불변 규칙'대로 `.env`. 확인은 사다리의 정적 분석에 묻어간다.
- **② 범위 점검 — 닿을 때만**: **요청 경로**(`create_client`·리다이렉트·`session_init_url`·GenericScraper의 URL 조립)를
  넣거나 고치는 커밋은 **그 변경 범위만** 점검한다 — GenericScraper는 소비자가 넘긴 임의 URL로 요청을 보내므로 SSRF의
  통로다(방어 판정은 BidWatch, 훅을 꽂을 자리는 이 패키지). `/security-review`, 못 쓰면 `노하우_설계.md` 목록으로 직접 — 결과는 커밋 메시지에.
- **③ 전체 리뷰 — BidWatch 공개 전 1회**: bidwatch 배포 Phase의 전체 리뷰 전에 이 패키지의 요청 경로 전체를 점검한다.

## 구조
- **디렉토리 뼈대 (파일 생성 위치의 최종 기준)**:
  ```
  bid_collectors/       ← 패키지 (수집기 1개 = 모듈 1개, utils/ = 날짜·텍스트·상태·HTTP)
  tests/                ← 패키지 테스트 (pyproject testpaths — 고정 응답 + -m integration 실호출)
  tools_tests/          ← 동봉 파이썬 도구(훅·측정기·규칙 개수) 테스트 — 패키지 테스트와 섞지 않는다
  docs/                 ← REQUIREMENTS.md, CONTRACT.md, interface.md, 설계 문서, playbooks/, archive/, handover/(버전별 bidwatch 인계서)
  work_log/             ← plan.md, Phase_XXX.md
  scripts/              ← 동봉 측정·점검 스크립트 (_tmp/ = 일회성, gitignore)
  ```
  **이 트리 밖에 새 최상위 디렉토리/파일을 만들지 말 것.** 필요하면 사용자 확인 후 이 목록부터 갱신.
- 설정/환경: `.env`(로컬, gitignore) / 키 목록은 `.env.example`. **통째 복사 절대 금지**, 필요한 키만 한 줄씩.
- 새 수집기 추가 위치 규칙: `bid_collectors/<출처>.py`에 `BaseCollector` 상속 + `_fetch()`만 구현, `__init__.py`의 `__all__`에 export,
  `tests/test_<출처>.py`(respx 고정 응답) + 통합 테스트 1건. 체크리스트: `work_log/Phase_002_review.md` §5-3.
- **같은 규칙이 두 곳 이상에 구현되면** 그 사실을 여기 적고, **두 구현을 대조하는 테스트를 반드시 만든다.**
  현재: XML 응답 파싱(`nara._parse_xml_items` ↔ `smes._parse_xml_response` — 같은 로직 복제, 대조 테스트 없음) ·
  `docs/interface.md`(이 저장소 ↔ bidwatch).

## 테스트 규칙
- 패키지 전체: `.venv/Scripts/python.exe -m pytest tests` (통합 테스트 포함 — **실제 API를 부르고 일 한도를 쓴다**)
  루틴: `.venv/Scripts/python.exe -m pytest tests -m "not integration"` · 정적 분석: `.venv/Scripts/python.exe -m ruff check bid_collectors tests`
- 동봉 파이썬 도구(훅·측정기)의 테스트: `python -m pytest tools_tests` — **훅·측정기·CLAUDE.md를 고쳤을 때만**.
- **수용 기준 = 테스트 케이스**: REQUIREMENTS.md의 각 `[ ]` 항목이 테스트 하나에 대응해야 한다.
- 외부 API에 의존하는 테스트는 **respx 고정 응답으로 도는 것을 기본**으로 두고, 실호출은 `@pytest.mark.integration`으로 분리한다.
- **픽스처 날짜는 실행 시점 기준 상대값으로** — 고정 날짜는 cutoff(실행 시점 상대)에 걸려 시간이 지나면 깨진다(27건 실사례, Phase 004).
- respx mock이 모든 페이지에 같은 응답을 주면 `max_pages`까지 계속 요청한다 — 페이지별 응답을 다르게(`Phase_003.md` §7).

## 작업 로그 원칙 (`work_log/Phase_XXX.md`) ★이 템플릿에서 가장 값이 큰 부분
- **Phase 로그는 실패한 접근·버린 대안이 있을 때만, 그때 바로, 2절로 쓴다.** 없으면 파일을 만들지 않고
  plan.md 체크박스 옆에 완료일만(2026-09-23 bid-collectors 채택). 뒤늦게 커밋에서 복원하면 시행착오는 소실된다.
  ① 실패한 접근과 원인("X 시도 → 실패 → 원인 Y → Z로 해결", 엉뚱한 데서 찾은 오해 포함)
  ② 다음 세션이 알아야 할 것(결정 이유+버린 대안·외부 제약·운영 주의점 — 있는 것만).
  코드 구현 상세·git 히스토리·테스트 목록·검증 결과는 쓰지 않는다 — 코드는 What, 로그는 Why. 작성은 메인 agent가 한다.
- Phase 001~003 로그(`Phase_001.md` ~ `Phase_003_detail.md`)는 이식 전 형식(구현 상세 포함, 구 번호)이다 — 고치지 않고 이력으로 둔다.
- **로그에 적은 교훈은 저절로 다음 코드에 옮겨지지 않는다** — 가능하면 테스트·훅이
  잡는 구조로 강제하고, **문서는 구조로 강제할 수 없는 것에 쓴다.** 수동 등록부에는 대조 테스트를
  같은 커밋에. 실사례: `노하우_프로세스.md`

## 배포 체크리스트 ("배포" = 버전을 올려 push → BidWatch가 editable 설치로 받는다)
1. 검증 — **트랙별로 다르다**: 저위험은 커밋 때 사다리대로 한 것 위에 **아무것도 더 얹지 않는다**.
   일반 트랙(계약 변경)은 전체 테스트 + BidWatch 쪽 테스트 실측 + 되돌리기 경로.
2. 버전 — `pyproject.toml`의 `version`과 `bid_collectors/__init__.py`의 `__version__`을 **둘 다** 올린다(CONTRACT.md 버전 규칙).
3. **handover 문서 작성** — `docs/handover/v<버전>.md`(템플릿 `_TEMPLATE.md`): 바뀐 것·`docs/interface.md` diff·bidwatch가 고칠 파일·검증법·되돌리기.
   bidwatch 쪽 파일은 고치지 않는다 — 반영은 bidwatch 세션 몫.
4. **실제 동작 확인(읽기·실행만)** — bidwatch의 `backend/.venv/Scripts/python.exe -m pytest backend/tests` 통과(같은 체크아웃을 editable로 설치).
5. commit + push

## 알려진 함정 (스택 중립 + bid-collectors에서 겪은 것. 새로 겪으면 `_TEMPLATE.md` 형식으로 플레이북 추가 + 여기 한 줄)

- **명령 앞에 무엇도 붙이지 말 것 — 한 호출 = 한 명령**(cd 접두사는 훅이 막는다). **허용된 앞머리로
  뒤엣것을 통과시키는 우회 금지 — 속도보다 우선.** 환경변수는 `settings.json`의 `env`에.
  상세: `노하우_승인_대기_최소화.md` §4
- **문서에 적힌 API·필드를 기억으로 쓰지 말 것**: 실제로 열어 이름·건수를 확인한다.
  200이 성공이 아닐 수 있다(data.go.kr은 200에 `resultCode` 에러를 싣는다). 상세: `노하우_외부API_데이터.md`
- **같은 원인의 버그를 고칠 때 다른 층에 같은 판정이 있는지 함께 본다** — 수집기 5개가 같은 루프를 복제했다.
- **data.go.kr 파라미터는 `serviceKey`(소문자 s)** — 나라장터 사전규격 API만 `ServiceKey`, 나라장터는 `inqryDiv=1` 필수.
  서비스마다 별도 활용 신청(보조금24 15113968, 낙찰·계약·사전규격) — 미신청 시 "등록되지 않은 인증키"·500 (`Phase_002.md` §9, `Phase_003.md` §11).
- **API를 curl로 시험하지 말 것** — 인코딩·파라미터 인코딩 차이로 404·한글 깨짐이 나서 오진한다. 파이썬(httpx)으로 (`Phase_002.md` §9).
- **숫자 필드에 `or` 금지**(`budget or est_price`는 0원을 버린다) · extra 필터는 `if v is not None and v != ""` (`Phase_002_review.md` §5).
- **cutoff는 자정으로 절삭한다** — `now - timedelta(days)`는 시각을 포함해 당일 공고가 빠진다(`Phase_002_review.md` §5-2).
- **서버 날짜 필터는 API 값 형식 그대로 보내고 건수로 확인한다** — odcloud `cond[...::GTE]`는 문자열 비교라 형식이 다르면 조용히 무력화된다(보조금24 `YYYYMMDDHHMMSS`, `Phase_004.md` ①).
- **httpx에 커스텀 transport를 넘기면 클라이언트의 `verify` 등 transport 인자가 무시된다** — `create_client`가 transport로 옮긴다(`152f930`).

## CLAUDE.md 비대화 방지 ★이 파일에는 상한이 있다

- **이 파일은 매 세션의 컨텍스트에 통째로 실린다** — 규칙이 많을수록 규칙별 준수율이 떨어진다.
  단위는 바이트가 아니라 **개수**다(IFScale 2025, Claude Code 세션 1,650건 요인 연구 2026).
- **상한: 규칙 100개**. 센다: `python scripts/count_claude_rules.py` —
  글머리·번호·표 행 하나 = 규칙 하나, `<...>` 플레이스홀더는 제외.
  Phase 종료 시 확인하고, 넘으면 이관한다:
  ① 구조(훅·테스트)로 강제된 항목은 한 줄+링크로 ② 정정의 역사·실험 전말은 플레이북으로
  (본문엔 현재 결론만) ③ 기능 상세는 REQUIREMENTS·Phase 로그로, 진행 서사는 plan.md로.
  남기는 것은 **"매 작업마다 적용되는 규칙 + 다음 세션이 첫 10분에 알아야 할 것"** 뿐이다.
- **중요한 규칙은 앞에 둔다.** ① 어기면 되돌리기 비싸고 구조(훅·테스트)로 강제되지 않는 것
  ② 매 작업마다 적용되는 것 ③ 참조용은 가운데 ④ 특정 시점에 찾아 읽는 것은 끝.
- **규칙은 빼기만 한다.** 한 번 통과하면 끝나는 게이트에는 모래시계(⏳) 만료 표시를 붙이고, 조건이
  채워지면 그 문장을 지운다. 만료 대상은 `count_claude_rules.py`가 Phase 종료 점검 때 함께 출력한다.
- **덧붙일 때 나갈 것을 함께 정한다.** **규칙·결론을 바꿨으면 같은 커밋에서 어긋난 문서를 grep으로
  대조한다 — 범위에 `docs/`만이 아니라 `.claude/skills/`·`.claude/agents/`를 넣는다.**
