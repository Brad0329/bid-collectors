# handover v1.3.1 — bid-collectors → BidWatch (작성 2026-09-25)

> **규칙(2026-09-25 사용자 결정)**: bid-collectors 세션은 bidwatch 폴더의 파일을 수정하지 않는다. 버전 작업을 끝내면 이 문서를
> `docs/handover/v<버전>.md`로 만들어(한 버전에 하나) 변경 상황과 bidwatch가 할 일을 넘긴다. 반영은 bidwatch 세션이 하고,
> 끝나면 §5를 채운다(그쪽 세션이 이 파일을 고쳐도 된다). 이 템플릿을 복사해 쓰고, 해당 없는 절은 "없음"이라고 적는다 — 지우지 않는다.

## 1. 무엇이 바뀌었나
- 버전 · 커밋 · Phase: **1.3.1** · `d157609`(수정)·`17a044b`(문서)·이 문서와 같은 커밋(버전) · plan.md Phase 008.
  출처: bidwatch `docs/source_fields.md` **부록 A #1·#3·#4·#9·#10**. 나머지 #2·#5·#6·#7·#8은 표준 필드 값이 바뀌어 일반 트랙이다. 이번 범위가 아니다(원칙 ② Phase).
- **계약**: 불변 → patch. `Notice`·`CollectResult`·시그니처·`bid_no` 형식·공개 export는 그대로다. `docs/CONTRACT.md`에 행이 없다(계약 변경이 아니다).
- **동작**:
  | 부록 A | 수집기 | 바뀐 것 | BidWatch가 받는 값 |
  |---|---|---|---|
  | #1 | `NaraCollector.collect_contracts` | 제목 = `cntrctNm` → 없으면 `cnstwkNm`. 공사 계약의 제목 태그는 `cnstwkNm`이다. 종전엔 공사가 **전건** 건너뛰어졌다 | 미사용 메서드라 영향 없음. 쓰면 공사 계약이 나온다. 실측 1일 0건 → 61~62건, 3일 10,572건 |
  | #3 | `NaraCollector.collect` | 공고첨부 `bidNtceFlNm/Url{i}` 루프를 삭제했다. 명세와 실측 3,360건 어디에도 없는 태그다. 규격서 `ntceSpecDocUrl` 첨부는 그대로다 | **불변**. 원래 0건이었다 |
  | #4 | `SmesCollector` | budget ← `suptScale` 손 매핑을 삭제했다. 없는 태그다 | **불변**. budget은 원래 항상 None이었다 |
  | #9 | `KstartupCollector` | 페이지 종료 판정과 절단 문구의 "전체 N건"이 `totalCount`(필터와 무관한 전체 30,168)에서 `matchCount`(진행중 230)로 바뀌었다. odcloud `code<0` 응답은 이제 `errors`로 온다 | 공고 값은 **불변**(실측 228건 동일). 요청은 1회 줄었다(4→3). `max_pages` 절단 문구가 "전체 30168건 중"에서 "전체 230건 중"으로 바뀐다 |
  | #10 | 문서 | `docs/bid_collectors.md`의 `smes24.py`, `dev_reference.md`의 `mss_biz.py` → `smes.py`로 대응 표시 | 없음 |
- **`docs/interface.md` diff**: 없음.

## 2. bidwatch가 해야 할 일
| # | 파일(bidwatch 기준 경로) | 무엇을 | 왜 | 필수/선택 |
|---|---|---|---|---|
| 1 | `docs/source_fields.md` 부록 A | #1·#3·#4·#9·#10을 "v1.3.1에서 해소"로 표시한다. §4 계약 요약의 "공사 전용 키는 결함 때문에 도달하지 않는다(부록 A-1)"도 고친다. §2 B의 attachments 행은 이미 규격서만 적혀 있어 그대로 둔다 | 필드 사전은 bid-collectors 동작을 따라가야 한다(문서 머리말 규칙) | 필수 |
| 2 | 코드 | 없음. BidWatch는 `collect_contracts`를 부르지 않는다. K-Startup·중소벤처·나라장터 입찰의 공고 값도 바뀌지 않았다 | — | — |

## 3. 검증 방법
- `pip install -e` 재실행: 메타데이터 버전(1.3.0)과 `bid_collectors.__version__`(1.3.1)을 맞추려면 한 번 다시 설치한다. 코드는 editable이라 이미 반영돼 있다.
- `backend/.venv/Scripts/python.exe -m pytest backend/tests`: 기대는 전부 통과다. 2026-09-25 bid-collectors QA 게이트에서 이 코드로 130 passed를 확인했다. 기대값이 바뀌는 테스트는 없다.
- 실테스트: 필요 없다. 화면에서 바뀌는 값이 없다. K-Startup 정기 수집 로그에서 요청이 1회 줄어든 것만 보인다.

## 4. 되돌리기
- 이전 버전은 `2491646`(v1.3.0)이다. editable 설치라 bid-collectors 체크아웃을 그 커밋으로 바꾸면 돌아간다. bidwatch 쪽은 문서 표시만 되돌리면 된다.

## 5. 반영 확인 (bidwatch 세션이 채운다)
- [ ] 반영 커밋: · 날짜: · 남은 것:
