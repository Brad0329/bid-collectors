# handover v1.4.0 — bid-collectors → BidWatch (작성 2026-09-26)

> **규칙(2026-09-25 사용자 결정)**: bid-collectors 세션은 bidwatch 폴더의 파일을 수정하지 않는다. 버전 작업을 끝내면 이 문서를
> `docs/handover/v<버전>.md`로 만들어(한 버전에 하나) 변경 상황과 bidwatch가 할 일을 넘긴다. 반영은 bidwatch 세션이 하고,
> 끝나면 §5를 채운다(그쪽 세션이 이 파일을 고쳐도 된다). 이 템플릿을 복사해 쓰고, 해당 없는 절은 "없음"이라고 적는다 — 지우지 않는다.

## 1. 무엇이 바뀌었나
- 버전 1.3.1 → **1.4.0** · 커밋 `10ce7a4`(LH+공통 헬퍼)·`23cdea0`(가스)·`1009bc0`(d2b)·`9038c3c`(수자원)·이 커밋(문서·버전) · Phase 009(F-011~F-014).
  요청서: bidwatch `docs/requests/bid-collectors_institution_collectors.md` ② 구현. 조사: bid-collectors `docs/institution_sources.md`.
- **계약**: 추가만(minor) — 공개 export 4개 `LhCollector`·`KogasCollector`·`D2bCollector`·`KwaterCollector`와 새 bid_no 형식
  (`docs/CONTRACT.md` 2026-09-26 행, 사용자 확인). 기존 수집기·`Notice`·`CollectResult`·시그니처 불변.

  | 수집기 | source | bid_no | organization | category | start/end | url |
  |---|---|---|---|---|---|---|
  | `LhCollector` | `LH` | `LH-{bidNum}` — 정정·취소는 **같은 bid_no**로 `bidDegree`·공고일이 갱신됨(upsert가 덮어씀) | 한국토지주택공사 | 시설공사·용역·지급자재·물품 | 공고일 / 입찰서 접수 마감 | LH 전자입찰 상세(물품은 첫 화면) |
  | `KogasCollector` | `가스공사` | `KOGAS-{NOTICE_CODE}`(10자리) | 한국가스공사 | 물품·용역·공사 | 공고일 / 마감 | 가스공사 전자입찰 상세 |
  | `D2bCollector` | `국방전자조달` | `D2B-{구분}-{키}-{차수}` — 구분 국내경쟁·국외경쟁·시설경쟁·국내수의·시설수의, 정정·취소는 **차수가 올라 새 bid_no** | 발주기관 `ornt`(국외경쟁은 "방위사업청") | 물품·공사·용역 | 공고일(국외·수의 2종은 **None**) / 입찰서·견적서 마감 | d2b 첫 화면(상세 링크 없음) |
  | `KwaterCollector` | `수자원공사` | `KWATER-{tndrPbanno}` | 한국수자원공사 | 공사·용역·물품·내자 | 공고일 / 마감(**약 65%가 None** — 원문 `-`) | 수자원 전자입찰 상세 |

- **동작(실측 2026-09-26, `DATA_GO_KR_KEY2`)**: 전부 `budget=None` — 금액은 `extra`에 원래 이름으로(LH `presmtPrc` 추정가격·`designPrc` 설계가·
  `fdmtlAmt` 기초금액, d2b `bsicExpt` 기초예비가격·`baseAmnt`·`budgetAmount` 예산, 수자원 `tndrPlnprc`(0 = 미공개가 절반), 가스공사는 금액 없음).
  취소 표시는 `extra` 원문(가스 `CANCEL_YN`, d2b `pblancSe`/`pblancCanclAt`, LH `bidKind`) — `status`는 기존 수집기와 같이 마감일 판정.

  | 수집기 | 1회 수집 호출 수 | 실측 건수 | 한도(오퍼레이션별) |
  |---|---|---|---|
  | LH | 1 (days 무관, 1000건/페이지) | days=14 → 127건 | 1,000/일 |
  | 가스공사 | 1 | days=14 → 96건 | 10,000/일 |
  | 국방전자조달 | 5 (목록 5종 × 1) | days=7 → 418건(경쟁 3종 130 + 진행 중 수의 2종 288) | **100/일 — 목록마다** |
  | 수자원공사 | 4 × 달 수(기준일이 든 달~이번 달, days=14면 대개 4~8) | days=14 → 154건 | 10,000/일 |

  data.go.kr 한도는 **오퍼레이션별**이다(응답 헤더 `x-ratelimit-remaining` 실측) — 기존 수집기(나라장터 등)와 나눠 쓰지 않는다. 요청서의 "1,000회 공유" 전제는 틀렸다.
  새 errors 문구: `[국방전자조달 {구분}] 페이지 1 요청 실패…`(목록 하나만 실패, 나머지 보존) · `[수자원공사 {op} {YYYYMM}] 페이지 정렬 불안정 — N행 중 M행이 겹침…`
  (한 달치가 한 페이지 응답 크기를 넘을 때만 — 지금 규모에선 안 난다) · `빈 응답 본문(HTTP 200)…`(수자원 응답 크기 초과).
- **`docs/interface.md` diff**: §1 필드 규칙 표 `bid_no` 예시 행(기관 4종 예시 추가) · §5 수집기별 환경변수 표에 4행 + "기관 수집기 4종 공통" 문단 추가,
  "공기업 API 5종 미구현" 문장 교체 · §6 "v1.3.0 기준" → "v1.4.0 기준". 이 저장소의 파일을 그대로 복사하면 된다.

## 2. bidwatch가 해야 할 일
| # | 파일(bidwatch 기준 경로) | 무엇을 | 왜 | 필수/선택 |
|---|---|---|---|---|
| 1 | bidwatch가 쓰는 `.env`의 data.go.kr 키 | 그 키로 15159012·15157366·15158416·15101635 **활용신청** 확인(bid-collectors 쪽 `DATA_GO_KR_KEY`·`DATA_GO_KR_KEY2`는 2026-09-26 둘 다 인증 통과) | 미신청이면 HTTP 403 → `errors`에 "페이지 1 요청 실패: HTTPStatusError" | 필수 |
| 2 | `system_sources` 마이그레이션(알리오 005와 같은 방식) · `collect_api.py` 매핑 | 출처 4개 추가 — source 문자열은 위 표 그대로(`LH`·`가스공사`·`국방전자조달`·`수자원공사`) | 요청서 §4 | 필수(스키마 게이트 — 사용자 확인) |
| 3 | 정기 수집 `days` | 권장: LH 7 · 가스공사 14(취소는 공고일 그대로 `CANCEL_YN`만 바뀐다 — 마감 기간을 덮어야 취소 표시를 받는다) · d2b 1~7(수의 2종은 days 무관 진행 중 전량) · 수자원 7~14 | 측정일이 추석 연휴라 **평일 등록 지연은 미측정** — 넉넉히 겹쳐 받고 upsert로 흡수 | 필수 |
| 4 | 공고 상세 화면(`NoticeModal.tsx` 등) | **금액을 이름별로 표시, 없는 이름은 `-`**(사용자 요청 2026-09-26): LH 추정가격 `presmtPrc`·설계가 `designPrc`·기초금액 `fdmtlAmt`·부가세 `addtTax` / d2b 기초예비가격 `bsicExpt`·기초금액 `baseAmnt`·예산 `budgetAmount` / 수자원 예정가격 `tndrPlnprc`(0이면 `-`로 볼지 결정) / 가스공사 없음. 그 밖에 계약방법(LH `tndrCtrctMedCd`·가스 `CONT_METHOD_NAME`·d2b `cntrctMth`·수자원 `ctrmthdCdNm`)·LH 지역제한 `zoneRstrct1~4`·면허 `req*Reqlic*Nm` | `budget`은 None(어느 금액을 budget으로 볼지는 원칙 ② 결정 전) — 값은 extra에 원문 | 필수(사용자 요청) |
| 5 | F-014 중복 공고 묶음(설계 시) | 알리오와 잇는 키: LH = 알리오 상세 `refrUrl`의 `bidNum=(\d{7})` ↔ `LH-{bidNum}` · 가스 = `refrUrl` `notice_code=(\d{10})` ↔ `KOGAS-` · 수자원 = 알리오 제목 `^\[([A-Z]\d{10})\]` ↔ `KWATER-`(취소 제외 28/28 일치) · d2b는 알리오에 없음 | 기관 수집기가 들어오면 같은 공고가 알리오와 두 번 보인다 | 선택(F-014 때) |
| 6 | 알리오 LH 링크 표시 | 알리오 LH `refrUrl`은 `https://` 없이 온다(`ebid.lh.or.kr/...`) · 가스 refrUrl 일부는 `&amp;amp;` 이중 이스케이프 | 그대로 링크로 쓰면 상대 경로·깨진 파라미터 | 선택 |
| 7 | `docs/interface.md` | 이 저장소 파일로 교체(§1 diff) | 양쪽 문서 동일 유지 | 필수 |
| 8 | 수집 이력·알림 | d2b는 목록 하나 실패해도 나머지가 오고 errors에 목록 이름이 붙는다 — 기존 is_partial 처리 그대로 | 참고 | 선택 |

## 3. 검증 방법
- `pip install -e <bid-collectors 체크아웃>` **재실행 필요**(메타데이터 1.3.1 → 1.4.0; 코드는 editable이라 이미 반영).
- `backend/.venv/Scripts/python.exe -m pytest backend/tests` — 기대: 전부 통과(bid-collectors 세션에서 이 체크아웃으로 132 passed, 2026-09-26). 바뀌는 기대값 없음.
- 실테스트: 4개 수집기를 bidwatch 키로 `collect(days=7)` → 각각 `errors == []`·건수 ≥ 1·`total_fetched == total_after_dedup`
  (bid-collectors `tests/test_integration_institutions.py`가 같은 기준 — 2026-09-26 4 passed). 상세 화면에서 금액 이름별 표시·`-` 확인.

## 4. 되돌리기
- 이전 버전: `df4080a`(Phase 009 코드 직전 — 코드는 v1.3.1 그대로) — 이번 변경은 추가만이라 bidwatch가 새 출처를 등록하지 않으면 영향이 없다.
  bidwatch 쪽을 되돌릴 땐 추가한 `system_sources` 행·매핑만 빼면 된다(이미 저장된 행은 source로 지울 수 있다).

## 5. 반영 확인 (bidwatch 세션이 채운다)
- [ ] 반영 커밋: · 날짜: · 남은 것:
