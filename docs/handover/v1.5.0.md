# handover v1.5.0 — bid-collectors → BidWatch (작성 2026-09-26)

> **규칙(2026-09-25 사용자 결정)**: bid-collectors 세션은 bidwatch 폴더의 파일을 수정하지 않는다. 버전 작업을 끝내면 이 문서를
> `docs/handover/v<버전>.md`로 만들어(한 버전에 하나) 변경 상황과 bidwatch가 할 일을 넘긴다. 반영은 bidwatch 세션이 하고,
> 끝나면 §5를 채운다(그쪽 세션이 이 파일을 고쳐도 된다). 이 템플릿을 복사해 쓰고, 해당 없는 절은 "없음"이라고 적는다 — 지우지 않는다.

## 1. 무엇이 바뀌었나
- 버전 1.5.0 · 커밋 `7587bae`(계약 게이트)·`c885f52`(수자원)·`b3391a2`(d2b)·`5c80f7a`(가스)·`05c1037`(LH)·`8631bd9`(LH 물품 url)·마감 커밋 ·
  Phase 010(plan.md). 요청서: bidwatch `docs/requests/bid-collectors_institution_fetch_detail.md`.
- **계약**: 시그니처·`bid_no` 형식·export 불변. 기관 수집기 4종의 `fetch_detail(bid_no)` 반환이 **None → dict**(기능 추가, minor) —
  `docs/CONTRACT.md` 2026-09-26 v1.5.0 행(사용자 확인: d2b 시설경쟁·수의 2종 = 목록 조회 후 상세 · LH TLS = 중간 인증서 동봉).
- **모양(알리오 v1.3.0과 같다)**: 원천의 비어 있지 않은 필드 전부·원래 이름(0 포함, 값은 원문 그대로) + `attachments: [{"name","url"}]`(없으면 `[]`)
  + `content`(**넷 다 `""`** — 원천에 공고 본문 필드가 없다. 공고문은 첨부 hwp). **실패·없는 공고는 예외**(빈 dict를 주지 않는다).

  | 기관 | 원천 | 호출 | 주요 키(원문 이름) | 첨부 |
  |---|---|---|---|---|
  | 수자원 | 사이트 내부 JSON(비공식) | 1 | `rqestAmt`(요청금액)·`tndrPlnprc`·`tndrMthNm`·`sucbidrDcsnMthCdNm`·`intnChargerEmail`·`intnChargerTelno`·`tndrPlaceInfo`·`tndrQualfAtpn`·**`tndrPrgsOrdrList`**(입찰 일정 list — `prgsDivNm`·`strtDt`·`closDt` YYYYMMDDHHMM)·`atchflList`(원문) | 2~6건, name = 파일명 |
  | 국방 d2b | 공식 상세 API | 경쟁 2종 1 / **시설경쟁·국내수의·시설수의 2** | `estmPrce`·`scsbidLwltRt`·`asessRtLwlt`/`Uplmt`·`sucbidrDecsnMth`·`chargerNm`·`chargerCttpc`·`bidPlace` / 시설: `lc`(현장 위치)·`areaLmttList`·`lcnsLmttList`(`^` 구분 문자열 원문)·원가 항목 | 없음 — `[]` |
  | 가스공사 | 사이트 HTML(비공식) | 1 | **화면 항목명**: `추정가격`·`부가세`·`합계금액`·`계약방법`·`계약담당(공고등록,개찰)`·`계약담당 연락처`·`납품장소`·`계약기간`·`입찰마감`/`입찰신청및입찰마감일시`(공고 부류마다 이름이 다르다)·`개찰일시`·`5. 업체제시문` + `진행상태`·`진행안내` + `품목내역`(list) | 페이지 내려받기 링크 전부 — 공고 첨부(`bid_download_attfile`)·**표준 계약조건**(`bid_download_rule_proc`, 거의 모든 공고에 같은 파일 9개 안팎)·구매요청 |
  | LH | 사이트 HTML(비공식) | 2(검색+상세) | **`표 이름/항목명`**: `공고일반정보/공고부서`·`공고일반정보/공고종류`·`계약및입찰방식정보/입찰방식`·`입찰진행정보/재입찰`·`입찰진행정보/입찰서접수마감일시`·`투찰제한정보/참가지역1`(현장설명 일시·장소는 그 표가 있는 17건(표본 23건 중 용역 제외) 모두 빈 값이라 거의 안 온다) + `요구면허`·`요구면허#2`·`파일정보`·`공고변경정보`(list) | 파일정보 행마다 1건 |

- **값의 주의점**: 가스 견적형 공고의 `추정가격`은 1·30·1,000 같은 가짜 값(본문에 "별도 산정 금액이 아님")이 13/26건 · 가스 빈 표기 `()`·`~`·`%,`가 원문 그대로 온다 ·
  LH `입찰공고건명`에 "본 공고는 지문인식 전자입찰…" 안내문이 붙어 온다 · 가스 취소 공고는 `진행상태`가 "공고중"으로 남고 `진행안내`만 "아래의 입찰이 취소되었습니다." ·
  수자원 목록 마감일 `-`(30일 358건 중 213건)인 공고도 상세 `tndrPrgsOrdrList`에 마감이 있다(`가격입찰서제출`·`견적제출및시담` 단계의 `closDt` — 24건 표본 전부).
- **첨부 `[]`의 의미**: "원천에 첨부가 없다"로만 온다(d2b는 첨부 필드 자체가 없어 늘 `[]`). 사이트 개편으로 첨부를 못 읽으면 `[]`가 아니라 예외다
  (가스 첨부 절·첨부 표 링크 수 대조, LH 파일정보 표 필수, 수자원 첨부 이름·ID 필수) — bidwatch가 `[]`를 "조회함"으로 저장해도 안전하다.
- **함께 바뀐 수집 값**: LH **물품** 공고의 `Notice.url`이 전자입찰 첫 화면 → 물품 상세 화면(`BidgdsDetailListCmd`)으로(`8631bd9`). 다른 업무·수집기 불변.
- **실측(2026-09-26)**: 수자원 8/8·d2b 10/10(5종×2)·가스 8/8·LH 8/8 성공, 저장 표본 가스 26·LH 23 파서 통과, 첨부 url 실제 GET 3기관 200 +
  Content-Disposition, 고장 6건(없는 번호 4기관·d2b 없는 수의·잘못된 키) 전부 예외(키 가림).
- **`docs/interface.md` diff**: §2 `fetch_detail` 설명에 "기관 수집기 4종(v1.5.0)" 항목 + 새 절 **"기관 수집기 `fetch_detail` (v1.5.0)"**(표 + 비공식·한도·첨부·LH TLS 주의) ·
  §5 표 4행 끝에 "`fetch_detail` (v1.5.0)" · §6 기준 버전 v1.5.0. bidwatch 쪽 같은 파일을 이 저장소 판으로 교체하면 된다.

## 2. bidwatch가 해야 할 일
| # | 파일(bidwatch 기준 경로) | 무엇을 | 왜 | 필수/선택 |
|---|---|---|---|---|
| 1 | (venv) | `pip install -e <bid-collectors>` 재실행 | 메타데이터 1.4.0 → 1.5.0 | 필수 |
| 2 | `backend/app/services/notice.py` | **코드 변경 없이 동작한다** — `enrich_notice_detail`은 수집기와 무관하게 `fetch_detail`을 부르고 예외는 경고 로그 후 None(다음에 재시도). 지금까지 4종은 None이라 `attachments`가 None으로 남아 **팝업마다 재호출**되고 있었다 → 설치 후 첫 팝업에서 1회 받고 저장. 확인만 | 요청서 §4 | 확인 |
| 3 | `frontend/.../NoticeModal.tsx` `InstitutionExtra` | 상세 키 표시 — §1 표의 원문 이름. 금액은 이름별(수자원 `rqestAmt`·d2b `estmPrce`·가스 `추정가격`/`합계금액`·LH `공고일반정보/추정가격`), 없으면 `-` · 입찰 일정(수자원 `tndrPrgsOrdrList`, 가스 `공고일시`/`개찰일시`, LH `입찰진행정보/*`) · 담당자 · 첨부 · d2b 시설 `areaLmttList`·`lcnsLmttList`(`^`로 나눠 표시) | 요청서 §4 | 필수 |
| 4 | 첨부 목록 화면 | 가스 첨부의 **표준 계약조건**(`bid_download_rule_proc`)을 공고 첨부와 나눠 보일지 결정 — 패키지는 원칙 ①대로 다 준다 | 공고마다 같은 9개 안팎이 섞인다 | 선택 |
| 5 | 첨부 내려받기(하게 되면) | LH 첨부는 **1GB 넘는 파일이 있다**(현장설명서 zip 1.2GB 실례) — 서버가 대신 받으면 스트리밍. LH 호스트는 서버가 중간 인증서를 안 보내 httpx 기본 검증이 실패한다 — 패키지 내부 `bid_collectors.lh.lh_ssl_context()`를 `verify=`로 쓸 수 있으나 계약 밖(공개 export 아님). 링크만 걸면 브라우저는 문제없다 | 메모리·TLS | 선택 |
| 6 | 호출 한도 | d2b는 오퍼레이션당 100회/일을 정기 수집과 **나눠 쓴다**. 경쟁 공고는 하루 신규 약 13건(국내 10·시설 3)이라 팝업만으로는 닿지 않지만, 시설경쟁·수의 2종 상세는 목록 오퍼레이션도 1회 쓴다 — 수의 진행 중 재고(국내 397·시설 520행)를 **미리 전량 조회하면 넘는다**. 팝업 1건씩만 부르면 안전 | 요청서 §3 | 확인 |
| 7 | `docs/interface.md` | 이 저장소 판으로 교체(§1 diff) | 계약 문서 동기화 | 필수 |
| 8 | (결정) | 수자원 목록 마감 `-` 공고를 상세 일정의 마감으로 보여 줄지·저장할지 — 요청서 §4대로 사용자에게. 패키지는 수집 시점에 채우지 않는다(원칙 ②) | 30일 59%가 `-` | 선택 |

## 3. 검증 방법
- `pip install -e` 재실행 후 `python -c "import bid_collectors; print(bid_collectors.__version__)"` = 1.5.0
- `backend/.venv/Scripts/python.exe -m pytest backend/tests` — 기대: 전부 통과(bid-collectors 세션에서 editable로 실행 확인 — §5 위 마감 커밋 메시지)
- 실테스트: 네 출처 공고 팝업을 하나씩 열어 ① 첨부가 보이고 링크가 파일로 받아지는지 ② 금액·일정·담당자 표시 ③ 같은 팝업을 다시 열 때 재호출이 없는지(`attachments`가 []여도 "조회함") ④ 사용자 제보 공고 `KWATER-B3202603397`(송산글로벌교육센터 수직정원) — 첨부 5건·요청금액·일정 5단계.
- 고장 확인: 없는 bid_no로 `fetch_detail` → 예외 → 경고 로그, 팝업은 뜬다.

## 4. 되돌리기
- 이전 버전: 커밋 `0619e02`(v1.4.0 마지막) — editable 설치라 bid-collectors 체크아웃을 그 커밋으로 바꾸면 4종 `fetch_detail`이 다시 None이 된다.
  bidwatch 쪽: 이미 저장된 `attachments`·`extra` 병합 값은 남는다(표시만 빼면 된다). v1.4.0 태그는 없다(v1.3.0까지 태그).

## 5. 반영 확인 (bidwatch 세션이 채운다)
- [ ] 반영 커밋: · 날짜: · 남은 것:
