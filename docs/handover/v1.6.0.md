# handover v1.6.0 — bid-collectors → BidWatch (작성 2026-09-26)

> **규칙(2026-09-25 사용자 결정)**: bid-collectors 세션은 bidwatch 폴더의 파일을 수정하지 않는다. 버전 작업을 끝내면 이 문서를
> `docs/handover/v<버전>.md`로 만들어(한 버전에 하나) 변경 상황과 bidwatch가 할 일을 넘긴다. 반영은 bidwatch 세션이 하고,
> 끝나면 §5를 채운다(그쪽 세션이 이 파일을 고쳐도 된다). 이 템플릿을 복사해 쓰고, 해당 없는 절은 "없음"이라고 적는다 — 지우지 않는다.

## 1. 무엇이 바뀌었나
- 버전 1.6.0 · 커밋 `a667b36`(계약 결정)·`2f9e8c4`(나라장터·K-Startup·중소벤처·보조금24·기업마당)·`2d6f2d5`(d2b url + 기관 4종·GenericScraper)·
  `1da9170`(사전규격)·`d396be2`(점+공백 날짜)·마감 커밋(d2b budget·spec-checker 반영) · Phase 011(plan.md). 요청서: bidwatch `docs/requests/bid-collectors_d2b_detail_url.md` + `docs/source_fields.md` 부록 A 남은 #2·#5·#6·#7·#8.
- **계약**: `Notice`·`CollectResult` 필드·타입·시그니처·`bid_no` 형식·export **불변**. 표준 필드 **값**이 바뀐다(원칙 ② — "출처가 준 값을 타입만 통일해
  담는다, 추정·대체·합성·절단·상수 없음") → minor. `docs/CONTRACT.md` 2026-09-26 Phase 011 행(사용자 확인 — 예외 4건 포함).
- **None이 되는 필드는 없다** — organization·region·category·url은 `""`, budget·end_date·start_date는 원래 Optional. bidwatch 응답 스키마(`str`)는 그대로 된다.

### 값 변화 (출처별)
| 출처 | 필드 | 종전 | v1.6.0 | 실측(2026-09-26) |
|---|---|---|---|---|
| 나라장터 입찰 | status | 마감일 판정만 | **`ntceKindNm`="취소공고" → `cancelled`** | 7일 5,224건 중 304건(용역 128·물품 93·공사 83) |
| 〃 | budget | 배정예산, 없으면 추정가격 | 배정예산만 — 용역·물품 `asignBdgtAmt`, **공사 `bdgtAmt`**(부록 A-5) | 공사 1,430/1,430건이 추정가격 → 배정예산으로 값이 바뀐다. 용역·물품은 배정예산이 늘 있어 불변(None 3/2,024) |
| 〃 | region | 수요기관명 `dminsttNm` | **공사 `cnstrtsiteRgnNm`**(현장 지역) · 용역·물품 **`""`**(지역 필드 없음 — 부록 A-6) | 용역·물품 3,794건 `""`, 공사 1,430건 현장 지역 |
| 〃 | category | 용역 "대 > 중" | 용역 대분류만(`pubPrcrmntLrgClsfcNm`, 중분류는 extra) · 물품·공사 불변 | |
| 나라장터 사전규격(`nara_prespec`) | title | 품명, 없으면 "사전규격 {번호}" | `prdctClsfcNoNm`만(값은 사업명) — 없으면 건너뜀(경고 로그) | 3일 222건 빈 값 0 |
| 〃 | category | 제목과 같은 값 | **`bsnsDivNm`**(업무 구분: 용역·물품·공사) | |
| 나라장터 낙찰·계약(bidwatch 미사용) | budget·start·end·region | 낙찰금액·실개찰일 폴백·총계약 폴백·계약기간·관할 구분 | None·최종낙찰일만·금차만·None·`""` | |
| 나라장터 계약 | status | 계약기간 문자열의 시작일로 판정 | end_date가 없어 **늘 ongoing**(마감일 판정 예외 — 계약에는 마감이 없다) | |
| K-Startup | organization | `pbanc_ntrp_nm` → `sprv_inst`(기관 유형) → "창업진흥원" | `pbanc_ntrp_nm`만, 없으면 `""`(부록 A-7) | 30일 121건 빈 값 0 |
| 〃 | content | 500자 절단 | **전문** | 121건 중 3건이 500자 초과 |
| 〃 | status | `rcrt_prgs_yn` 없으면 closed | 없으면 마감일 판정 | |
| 〃 | `only_ongoing` | 기본 True | **기본 True 유지**(원칙 ②의 명시적 예외 — 등록일 필드가 없고 서버 날짜 필터가 데이터에 먹지 않아 진행중이 유일한 범위 조건) | 불변 |
| 중소벤처 | organization · category · content | "중소벤처기업부" · 작성 부서 `writerPosition` · 500자 | **`""` · `""`** · 전문 | 7일 5건 |
| 보조금24 | content | "서비스목적요약 + 지원내용" | 서비스목적요약만(지원내용은 extra) | |
| 〃 | end_date | 신청기한 기간이면 **시작일**(부록 A-2) · "2026. 2. 1." 같은 점+공백 형식은 None | 기간이면 **끝 날짜** · 점+공백 형식도 읽는다(`d396be2`) | 7일 116건 중 기간형 24/24 끝 날짜(QA 독립 대조) |
| 기업마당 | region | 소관기관 `jrsdInsttNm`(부처명) | **`""`** | 3일 36건 전부 `""` |
| 〃 | start_date | `reqstBeginEndDe`가 날짜 하나면 그 날짜 | 기간("A ~ B")만 나눈다 — 날짜 하나는 start·end 둘 다 None | 36건 중 7건 None |
| LH | status · organization | 마감일만 · "한국토지주택공사" | **`bidKind`="취소공고" → `cancelled`** · **`""`** | 7일 52건: cancelled 2 |
| LH | category · url 보고 | 업무 구분이 모르는 값이면 조용히 첫 화면 | 문자열 `"null"`은 빈 값(category `""`) · 첫 화면으로 둔 공고는 **errors에 건수**(`is_partial=True`) | **2026-09-27 API가 업무 구분을 14일 127/127건 `"null"`로 준다**(전날 52/52 값 있음) → 지금은 LH url이 전부 첫 화면 + errors 1줄. `fetch_detail`은 검색으로 업무 코드를 얻어 정상 |
| 가스공사 | status · organization | 마감일만 · "한국가스공사" | **`CANCEL_YN`="취소" → `cancelled`** · **`""`** | 7일 54건: cancelled 3 |
| 수자원 | organization | "한국수자원공사" | **`""`** | 7일 51건 |
| d2b | **url** | 첫 화면 `https://www.d2b.go.kr/` | **사이트 상세 화면**(요청서 — 아래) | 7일 684건 결측 0 |
| 〃 | status | 마감일만 | 경쟁 `pblancSe`="취소공고"·수의 `progrsSttus`="공개협상취소" → `cancelled` | 684건 중 88건 |
| 〃 | organization(국외경쟁) | "방위사업청" | `""` | 4건 |
| 〃 | budget | None | **수의 2종 = 예산금액 `budgetAmount`**(사용자 확인) · 경쟁 3종 None(예산 필드 없음 — 기초금액·기초예비가격은 extra) | 수의 554/554건 값 있음 |
| 〃 | 수의 2종 조회 범위 | 견적서 마감 오늘~1년 뒤(진행 중 전량) | **(오늘-days)~1년 뒤** — 수집 기간 안에 마감된 공고도 온다 | days=7: 국내수의 116→227·시설수의 172→327(늘어난 건 closed) |
| 〃 | 국외 개찰일 범위 | 기준일~1년 뒤 | 기준일~2년 뒤 | 4건 동일 |
| GenericScraper | organization | `config.name` | `""`(사이트 이름은 `source`) | |

- **취소 판정이 bidwatch F-017과 겹친다** — 키·값을 bidwatch `services/collection.py` `_CANCEL_MARKS`와 같게 맞췄다(수자원은 취소 표시 필드가 없다 — 취소 공고가 API에서 빠진다).
  두 곳이 같은 판정을 하니 결과는 같다.
- **예외(원칙 ②를 적용하지 않은 것, 2026-09-26 사용자)**: ① ongoing/closed의 마감일 판정·낙찰 closed(편의 계산값 — 쓰지 말고 end_date로 계산 권장) ② 출처 ID로 만든 url·url 폴백
  ③ 첨부 이름 자리표시자(`규격서1`·`첨부파일1`) ④ K-Startup `only_ongoing=True`. GenericScraper status(게시일 판정)도 종전대로(2026-09-25 "안 함").

### d2b url (요청서 `bid-collectors_d2b_detail_url.md` 응답)
- 요청서 §2 틀 + **메뉴 고정 파라미터가 필수**였다 — 없으면 **쿠키 없는 첫 클릭에 500**(국내경쟁 0/89·시설경쟁 0/37·수의 0/18). bidwatch의 "30/30"은 한 세션으로 이어서 열어
  앞 화면이 세션 문맥을 채운 결과로 보인다. 붙인 값: 국내경쟁 `lv2Divs=1&pageDivs=G1&bid_divs=bid` · 시설경쟁 `lv2Divs=1&pageDivs=E1` · 국내수의 `pageDivs=G` · 시설수의 `pageDivs=E` · 국외 없음.
- **§3-① 지명경쟁**: 시설경쟁 4 + **국내경쟁 2**(요청서는 시설만 봤다) 모두 로그인 필요 → 목록 화면: 시설 `/peb/bid/announceList.do?key=41`, 국내경쟁은 `busiDivs`로 물품 `/pdb/bid/goodsBidAnnounceList.do?key=13`·
  용역 `/psb/bid/serviceBidAnnounceList.do?key=32`(쿠키 없이 200·로그인 문구 없음 확인, 목록 그리드는 JS라 렌더링은 미확인 — 브라우저로 봐 달라). 다른 계약방법에는 로그인 필요가 없었다(418행 전수).
- **§3-② iemNo `001` 행**: 원인은 iemNo가 아니라 위 메뉴 파라미터(세션 문맥) — `pageDivs=G`로 열린다. 폴백 불요.
- §2 추정 확인: 시설수의 `ordr_year`·국외 `grd_dprtCode`는 서버가 값을 검사하지 않는다(틀려도 열림) — JS와 같은 값(`cntrwkNo` 앞 4자·`pblancNo` 앞 3자)을 넣었다. 국외 `grd_bidxDate`는 틀리면 500.
- 미확인 → 확인: 차수 1·2는 **서로 다른 화면**(11/11 — 공고구분·차수 표기·첨부가 다르다).
- 주소 필드가 비면 첫 화면 url + errors `"[국방전자조달] 상세 화면 주소를 만들 수 없어 첫 화면으로 둔 공고 — 국내경쟁 N건 …"`(이때 `is_partial=True`). 7일 684건 결측 0.
- 코드가 만든 url을 쿠키 없이 GET: 36/36 본문에 건명(구분별 최대 8). `detail_url`은 `""`(요청서대로).
- **비공식 경로** — interface.md §5 아래 d2b url 항목에 적었다. 사이트 개편 시 빈 화면·500이 될 수 있고 실패가 200으로 온다(`negn_degr`가 틀리면 200 + 건명 없음).

### `docs/interface.md` diff
§1 Notice 주석(organization·end_date·content·budget·region·category) · §1 필드 규칙 `status` 행 교체 + **"표준 필드 원칙 (v1.6.0)" 행 신설** · `content` 행 ·
§5 d2b 행(수의 범위·url) · §5 아래 기관 수집기 공통 문단(organization `""`) + **d2b url 문단 신설** · §6 기준 버전 v1.6.0. bidwatch 쪽 같은 파일을 이 저장소 판으로 교체하면 된다.

## 2. bidwatch가 해야 할 일
| # | 파일(bidwatch 기준 경로) | 무엇을 | 왜 | 필수/선택 |
|---|---|---|---|---|
| 1 | (venv) | `pip install -e <bid-collectors>` 재실행 | 메타데이터 1.5.0 → 1.6.0 | 필수 |
| 2 | `docs/interface.md` | 이 저장소 판으로 교체(§1 diff) | 계약 문서 동기화 | 필수 |
| 3 | 발주기관 표시·검색 (`NoticeModal.tsx` "발주기관", `NoticeTable.tsx` 기관 클릭 검색, `routers/notices.py` q 검색) | LH·가스·수자원·중소벤처·d2b 국외·URL 출처의 organization이 `""`가 된다(upsert가 기존 행도 다음 수집에 덮는다) — 빈 값일 때 출처 이름을 보일지, 기관명 검색에서 빠져도 되는지 **결정**. 기관명이 필요하면 bidwatch가 출처→기관 대응을 갖는다(패키지는 상수를 넣지 않는다 — 사용자 결정) | 원칙 ② 상수 제거 | 필수(결정) |
| 4 | 지역 필터 (`services/region.py`·`routers/notices.py:166`) | 나라장터 용역·물품·기업마당 region이 `""` — 빈 값은 필터를 늘 통과하므로 **지역을 고르면 이 공고들이 전부 걸린다**(종전엔 수요기관명에 시·도 이름이 든 것만). 지역 필터 결과가 늘어나는 게 괜찮은지 결정. 공사는 이제 패키지가 `cnstrtsiteRgnNm`을 주므로 `notice_region`의 공사 덮어쓰기는 같은 값을 한 번 더 쓴다(지워도 된다) | 부록 A-6 | 필수(결정) / 덮어쓰기 제거 선택 |
| 5 | 예산 칸 (`NoticeModal.tsx` "예산") | 나라장터 공사 예산이 추정가격 → 배정예산 `bdgtAmt`(부가세 포함 큰 값)으로 바뀐다. d2b 수의 2종은 None → 예산금액(`budgetAmount`)이 들어온다 — 모달이 이름별로 따로 보이는 `budgetAmount`와 겹쳐 보일 수 있다. 표시 이름이 "예산"이면 그대로 맞다 | 부록 A-5 | 확인 |
| 6 | 취소 판정 (`services/collection.py` `_CANCEL_MARKS`) | 패키지가 같은 키·값으로 `cancelled`를 준다 — 그대로 둬도 결과가 같다. 한 곳으로 줄일지는 선택(패키지 쪽만 남기면 수자원·알리오·GenericScraper는 원래 판정 대상이 없다) | F-017과 중복 | 선택 |
| 7 | d2b 기존 행 url | upsert가 url을 갱신 — 수의 2종은 진행 중 전량이라 다음 수집에 전부 바뀌고, 경쟁 3종은 수집 `days` 밖의 기존 행이 첫 화면으로 남는다 → 넓은 `days`로 한 번 수집할지(d2b 목록 5종 × 1~2페이지, 한도 100회/일) | 요청서 §4 | 선택 |
| 8 | d2b 수의 closed 행 | 수의 2종에 수집 기간 안에 마감된 공고(closed)가 새로 들어온다(days=7에서 +266건) — 목록에 마감 필터가 없으니 그대로 보인다. 숨길지 결정 | 원칙 ② 기본 필터 제거 | 확인 |
| 9 | 키워드 매칭·정렬 | K-Startup·중소벤처 content가 전문이 되어(500자 초과 30일 3/121건) 키워드에 걸리는 공고가 조금 늘 수 있다. 보조금24 content는 요약만이 되어 조금 줄 수 있다. `services/notice.py:84`("content가 있으면 상세 조회 건너뜀")의 판정은 바뀌지 않는다(비었던 content가 채워지는 출처 없음) | 절단·합성 제거 | 확인 |
| 9-1 | LH 수집 이력 | 2026-09-27부터 LH 출처 API 이상(업무 구분 `"null"`)으로 정기 수집마다 errors "[LH] 업무 구분을 몰라 … 첫 화면으로 둔 공고 — 'null' N건"이 붙고 `is_partial=True`가 된다. 공고는 전부 들어온다. 알림에서 "부분 실패"로 띄울지 확인. 팝업 바로가기는 첫 화면이 된다 | 출처 변화(조용한 실패 방지) | 확인 |
| 10 | `docs/source_fields.md` 부록 A | #2·#5·#6·#7·#8 "v1.6.0에서 해소" 표시(#8은 패키지도 cancelled를 준다) · §1 출처별 원천 표(organization·region·budget·content) 갱신 | 문서 동기화 | 필수 |

## 3. 검증 방법
- `pip install -e` 재실행 후 `bid_collectors.__version__` = 1.6.0
- `backend/.venv/Scripts/python.exe -m pytest backend/tests` — 기대: 전부 통과(bid-collectors 세션에서 editable로 실행: 137 passed, 2026-09-26)
- 실테스트: ① d2b 5종 각 1건 + 지명경쟁 1건의 "공고 사이트 바로가기"가 어디로 열리는지(상세 화면 / 지명경쟁은 목록 화면) ② LH·가스 공고의 발주기관 칸(빈 값 처리 #3)
  ③ 지역 필터 "서울" 결과에 나라장터 용역이 늘어나는지(#4) ④ 나라장터 공사 공고의 예산 값이 배정예산인지 ⑤ 나라장터 취소공고가 "취소"로 보이는지(종전과 같아야 한다).
- 고장 확인: d2b 주소 필드가 빈 행이 오면 첫 화면 url + 수집 이력 errors에 "상세 화면 주소를 만들 수 없어…" 한 줄.

## 4. 되돌리기
- 이전 버전: v1.5.0 마지막 커밋 `d4486b8`(handover 기록)·코드 기준 `e8472d7` — editable 설치라 bid-collectors 체크아웃을 그 커밋으로 바꾸면 종전 값으로 돌아간다
  (다음 수집 upsert가 organization·region·budget·url을 다시 덮는다). bidwatch 쪽 #3·#4 처리를 넣었다면 그것만 되돌리면 된다. 태그는 v1.3.0까지.

## 5. 반영 확인 (bidwatch 세션이 채운다)
- [ ] 반영 커밋: · 날짜: · 남은 것:
